// Drizzle schema — STUB รอบนี้ยังไม่ออกแบบตารางจริง
// คาดว่าจะมี: users, banks, categories, cards, transactions, installments
// เงิน = decimal(12, 2) · เดือน = year (int) + month (int 1-12)
// admin tables (banks, categories, users) ไม่มี userId
// user tables (cards + transactional) มี userId

export {};
