# archive/

UI mock เก่าที่ใช้ตอนวาง concept ครั้งแรก — **เก็บไว้อ้างอิงเฉย ๆ ไม่ใช่ source of truth**

- `mock.html` — Tailwind CDN, dark theme, สำหรับ `/credit-cards` ก่อน wiring
- `prototype.jsx` — React + recharts + lucide, มี state mock ของหลายหน้า

ของจริงตอนนี้: shadcn/ui + Tailwind v4 + light theme (ดู `src/`) — UI ในไฟล์เหล่านี้ **ไม่ตรงกับโปรดักชันแล้ว** อย่าใช้เป็นต้นแบบของ component
